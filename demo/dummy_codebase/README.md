# predd-demo

A minimal Python API used for the predd demo. Intentionally incomplete — the open issues show what needs to be built.

## Open issues

| Issue | Summary |
|-------|---------|
| DEMO-10 | Add `/health` endpoint |
| DEMO-11 | Fix off-by-one in `paginate()` |
| DEMO-12 | Add unit tests for `/items` endpoint |

## Running

```bash
pip install -r requirements.txt
python app.py
```

## Tests

```bash
pytest tests/
```
